node_modules/
dist/
.DS_Store
*.log
.env
.env.local

# devintern-pm configuration (contains secrets)
.devintern-pm/.env
.devintern-pm/.auth-session.json

# @devintern/code - runtime state and secrets live here; only commit explicit config
.devintern-code/*
!.devintern-code/settings.json
!.devintern-code/.env.example

# Supabase CLI local cache (linked project ref, pooler URL, version info)
supabase/.temp/
packages/pm-desktop/out/

# Internal strategy docs — never publish
/docs/
packages/website/docs/
